import os


def generate_txt(file):
    ...


def fill_audio_lenght(file):
    # ffmpeg -f concat -safe 0 -i files.txt -c copy output.wav
    # https://trac.ffmpeg.org/wiki/Concatenate
    # return PATH of the filled file, e.g 'misc/new_file.wav'
    ...